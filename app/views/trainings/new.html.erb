<%= render 'shared/page_title', title: "Create a new Training" %>

<%= render 'shared/errors', obj: @training %>

<div class="row">
  <div class="well col-md-8 col-md-offset-2">
    <%= form_for @training do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>

      <%= f.submit "Create a Training", class: "btn btn-success" %>
    <% end %>
  </div>
</div>
